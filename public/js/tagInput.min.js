/**
 * Author: Peyman Nader
 * Author ULR: https://github.com/peymanath
 * Description: TagInput For TailwindCss
 */

const addNewTag = e => { e.preventDefault(); let t = $(".inputTagJS"), a = []; if (t.value.trim().length) { let s = $(".addTagJs").parentElement; s.remove(); let l = d.createElement("Tag"); l.classList.add("TagClass"), l.setAttribute("value", t.value), l.innerHTML = `<svg class="w-5 h-5"><use xlink:href="#times" class="removeTagJs cursor-pointer"></use></svg><span>${t.value}</span>`, $(".listTagsJs").append(l, s), t.value = "", t.focus() } else alert("لطفا در فیلد برچسب ها مقداری را وارد کنید."); $(".listTagsJs tag").length > 1 ? [...$(".listTagsJs tag")].forEach(e => { a.push(e.attributes[1].textContent) }) : a = $(".listTagsJs tag").attributes[1].textContent, $("#TagList").value = a }, reamoveTag = e => { let t = [...e.target.classList]; if (t.find(e => "removeTagJs" == e)) { let a = e.target.parentElement.parentElement; a.remove(); let s = $("#TagList").value.split(","), l = a.attributes[1].textContent; $("#TagList").value = s.filter(e => e !== l) } }; $(".addTagJs").addEventListener("submit", addNewTag), $(".listTagsJs").addEventListener("click", reamoveTag);